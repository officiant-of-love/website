const Hero = () => {
    return (
        <section className={`bg-wedding-hero absolute top-0 w-full h-[400px] bg-cover bg-center -z-10`}>
            Weddings
        </section>
    );
};

export default Hero;